name: Deploy Release Packages

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      container: ${{ steps.set.outputs.container }}
    steps:
      - name: Determine Container Tag.
        id: set
        run: echo "container=ghcr.io/oguztoraman/libmagicxx-dev:${GITHUB_REF_NAME%.*}.x" >> $GITHUB_OUTPUT
  release_packages:
    needs: setup
    runs-on: ubuntu-latest
    container:
      image: ${{ needs.setup.outputs.container }}
    steps:
      - name: Checkout.
        uses: actions/checkout@v4
        with:
          submodules: recursive
          lfs: true
      - name: Generate Release Packages.
        run: ./scripts/generate_release_packages.sh
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          name: Libmagicxx ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            ./packages/libmagicxx-*
